name: Trigger auto deployment for container-app-wsmlqclwinyum

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - 'server/**'
    - '.github/workflows/container-app-wsmlqclwinyum-AutoDeployTrigger-4ba9956e-01ad-4503-9903-5b5d86d7cb21.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPWSMLQCLWINYUM_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPWSMLQCLWINYUM_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPWSMLQCLWINYUM_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}/server 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPPWSMLQCLWINYUM_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPWSMLQCLWINYUM_REGISTRY_PASSWORD }}
          containerAppName: container-app-wsmlqclwinyum
          resourceGroup: todonodejspgsqlgoogleauth866592e6
          imageToBuild: default/container-app-wsmlqclwinyum:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


